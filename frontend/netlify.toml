[build]
  base = "frontend"
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"

[secrets_scan]
  omit_keys = [
    "VITE_FIREBASE_KEY",
    "VITE_UNSPLASH_ACCESS_KEY"
  ]